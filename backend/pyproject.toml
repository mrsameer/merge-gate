[project]
name = "mergegate"
version = "0.1.0"
description = "MergeGate control plane: LangGraph orchestrator, LLM-free acceptance engine, and API"
authors = [
    { name = "mrsameer", email = "mrsameer@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.139.2",
    "gitpython>=3.1.55",
    "langgraph>=1.2.9",
    "langgraph-checkpoint-sqlite>=3.1.0",
    "pydantic>=2.13.4",
    "pyyaml>=6.0.3",
    "sse-starlette>=3.4.6",
    "uvicorn>=0.51.0",
]

[project.scripts]
mergegate = "mergegate:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "anyio>=4.12.1",
    "httpx>=0.28.1",
    "pyright>=1.1.411",
    "pytest>=8.4.2",
    "ruff>=0.16.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/mergegate"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pyright]
include = ["src", "tests"]
pythonVersion = "3.11"
typeCheckingMode = "basic"
