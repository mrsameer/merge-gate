# The backend loads this file's local counterpart, backend/.env, on startup.
# Keep secrets only in backend/.env; never commit that file.

# Vertex AI Gemini CLI (Mumbai)
GOOGLE_GENAI_USE_VERTEXAI=true
GOOGLE_CLOUD_PROJECT="your-google-cloud-project"
GOOGLE_CLOUD_LOCATION=asia-south1

# Claude Code: set exactly one credential locally when needed.
# ANTHROPIC_API_KEY=""
# CLAUDE_CODE_OAUTH_TOKEN=""
